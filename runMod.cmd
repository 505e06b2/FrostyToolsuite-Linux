rem You could use an environment variable, but hardcoding here for consistency
rem The datapath can be relative to the game folder - it is enforced in the shim that the current directory is set there
"original_%~nx1" -datapath "LinuxMod"
